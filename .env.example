# Copy to .env and fill in values before running docker compose
POSTGRES_PASSWORD=codocs_secret
JWT_SECRET=change-me-in-production
INSTANCE_DOMAIN=localhost
INSTANCE_URL=http://localhost:8080
PORT=8080
